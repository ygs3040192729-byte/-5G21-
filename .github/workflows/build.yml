name: Daily Build Nokia 5G21 FM160 Firmware

on:
  workflow_dispatch:      # 保留手动触发按钮
  schedule:
    - cron: '0 20 * * *'  # 每天北京时间凌晨 4:00 自动执行 (UTC 20:00)

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Initialization environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools swig zlib1g-dev apktool

      - name: Clone source code
        run: |
          git clone --depth 1 https://github.com/coolsnowwolf/lede
          cp -r lede/* .

      - name: Update & Install feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Patch FM160-EAU Support (Kernel 5.4)
        run: |
          # 核心步骤：注入 FM160-EAU 的硬件 ID，否则无法识别模块
          sed -i '/{ USB_DEVICE(0x2cb7, 0x0104) }/a \	{ USB_DEVICE(0x2cb7, 0x01a0) }, /* Fibocom FM160-EAU */' target/linux/generic/files/drivers/usb/serial/option.c

      - name: Load Custom Configuration
        run: |
          # 自动配置：Nokia 5G21 型号、FM160 驱动、ModemManager 拨号插件、NSS 加速
          cat <<EOF > .config
          CONFIG_TARGET_ipq807x=y
          CONFIG_TARGET_ipq807x_generic_DEVICE_nokia_5g21=y
          CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=y
          CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y
          CONFIG_PACKAGE_kmod-usb-serial-option=y
          CONFIG_PACKAGE_luci-app-modemmanager=y
          CONFIG_PACKAGE_modemmanager=y
          CONFIG_PACKAGE_sms-tool=y
          CONFIG_PACKAGE_kmod-qca-nss-drv=y
          CONFIG_PACKAGE_kmod-qca-nss-ecm=y
          EOF
          make defconfig

      - name: Download package
        run: make download -j8

      - name: Compile the firmware
        run: make -j$(nproc) || make -j1 V=s

      - name: Upload firmware to Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Nokia-5G21-FM160-Daily
          path: bin/targets/ipq807x/generic/*.bin
